# Ralph Progress Log
Project: Asteroid Mining Prospector
Started: 2026-02-23

## Codebase Patterns
- ConfigArrays for vectorized MC: `build_config_arrays(config, mode)` returns a frozen `ConfigArrays` dataclass with flat `np.ndarray(17,)` arrays for density, grade, recoverability — indexed by Mahlke class index. Use fancy indexing: `ca.density_mean[class_indices]` for vectorized lookups.
- Config checksum gate: Scoring YAMLs in `scoring/` are locked by SHA-256 hashes in `prospector/scoring/config_schema.py:CONFIG_CHECKSUMS`. `load_config()` validates on every call. To modify a YAML: update the file, run `shasum -a 256 scoring/<file>.yaml`, update `CONFIG_CHECKSUMS`, and do it in a dedicated bead.
- Design by Contract: `import deal` — use `@deal.pre` for input domain, `@deal.post` for output invariants. Add `deal.cases(func, count=N)` in `tests/test_properties.py` for auto-generated tests. Contracts active in tests, disable in production with `deal.disable(permanent=True)`.
- Pipeline CLI: `python -m prospector --stages ingest spectral scoring output --data-dir data/ --mode earth_return`. Use `run_pipeline()` programmatically.
- FastAPI testing: Create `sqlite3.connect(":memory:", check_same_thread=False)` for test DB. Override `get_db` dependency: `app.dependency_overrides[get_db] = lambda: conn`. Use `TestClient(app)` from `fastapi.testclient`.
- Hypothesis property tests: Use `deadline=None` for compute-heavy tests (EVOI). Use paired RNG seeds `np.random.default_rng(seed)` for fair monotonicity comparisons. Class-level `_config = load_config()` avoids per-example YAML loading.
- Hypothesis stateful tests: `RuleBasedStateMachine` in `tests/test_stateful.py` — use `@initialize()` for DB setup, `@precondition` for state guards, `note()` for debug logs. Low `n_samples=50` for runtime. `settings(max_examples=50, stateful_step_count=15, deadline=None)`.
- EVOI computation is expensive (~2.4s per call at n_samples=30). Full `compute_evoi` should be tested with `deadline=None` and modest `max_examples`. Direct EVOI comparison (e.g., EVOI_vnir >= EVOI_vis) is unreliable at low n_samples due to MC noise — test mathematical prerequisites instead.
- Literal types: `ScoringMode` and `ObservationType` defined in `prospector/schemas.py` — use for all mode/observation parameters
- Package root: `prospector/` with subpackages `ingest/`, `spectral/`, `scoring/`, `ephemeris/`
- pytest configured via `pyproject.toml` with `testpaths = ["tests"]` and `pythonpath = ["."]`
- Scoring YAML configs go in `scoring/` directory (project root, not inside `prospector/`)
- Data files are gitignored (`data/`); create subdirs programmatically in ingest modules
- Python 3.11.8 available on system (no venv currently; `env/` in .gitignore)
- Database access: `prospector.db.get_connection(":memory:")` for tests, `get_connection()` for default file DB
- BLOB round-trip: `array.tobytes()` to store, `np.frombuffer(blob, dtype=np.float64)` to read
- Schema is idempotent (`CREATE TABLE IF NOT EXISTS`) — safe to call `init_schema()` multiple times
- Mutation testing gate: After closing a bead touching `prospector/`, run `scripts/mutmut-gate.sh prospector/path/to/module.py` — requires <10% surviving mutants
- SBDB ingest: `from prospector.ingest.sbdb import ingest_sbdb_csv` — takes csv_path + conn, returns row count
- SPK-ID mapping: numbered asteroids spkid ∈ [2000001,2999999] → asteroid_id = spkid - 2000000; others pass through
- Use `INSERT OR REPLACE` for idempotent ingestion — safe to re-run on same data
- Column names normalized to lowercase in ingest — handles case variations in CSV headers
- NEOWISE ingest: `from prospector.ingest.neowise import ingest_neowise` — takes filepath + conn, returns row count
- NEOWISE cross-match: IAU number first (from preloaded set), designation fallback (DB lookup)
- FK-safe ingestion: preload `known_ids = set(row[0] for row in conn.execute("SELECT asteroid_id FROM asteroids"))` before inserting into child tables
- Column alias resolution: `_resolve_columns()` pattern — try explicit map first, then known aliases list, case-insensitive
- astropy available via astroquery dep — `from astropy.io import ascii as astropy_ascii` for IPAC table format
- classy taxonomy: `pip install space-classy tf-keras 'tensorflow-probability==0.24.*'`; import as `import classy`
- classy API: `classy.Spectrum(wave=arr, refl=arr, pV=albedo)` — albedo MUST be `pV=`, NOT `albedo=`
- classy classification: `spec.classify(taxonomy='mahlke')` → `spec.class_mahlke` (label) + `spec.class_A..Z` (17 probabilities)
- classy Mahlke classes: A, B, C, Ch, D, E, K, L, M, O, P, Q, R, S, V, X, Z
- classy requires numpy<2.0 — use isolated venv; set ROCKS_CACHE_DIR env var for automated runs
- Entity resolver: `from prospector.ingest.entity_resolver import resolve` — takes any identifier string + optional conn, returns ResolvedID(asteroid_id, designation, packed, source)
- MITHNEOS ingest: `from prospector.ingest.mithneos import ingest_mithneos_dir` — takes dirpath + conn, returns spectrum count
- Spectral ingest pattern: read file → parse filename for identity → resolve to asteroid_id → FK check → store as numpy BLOBs
- Scoring modifiers: global modifiers (spin) multiply composite_score; material-specific modifiers (thermal depletion) multiply grade_frac inside the MC material loop
- Hapke model: `from prospector.spectral.hapke_model import model_asteroid, forward_model, fit_spectrum`
- Hapke endmembers: Endmember dataclass with n, k_baseline, bands (Gaussian absorption); default set OLIVINE/PYROXENE/PLAGIOCLASE/TROILITE/FE_NI_METAL
- Forward model parameter vector: [4 fracs, 5 grain_sizes, 1 smfe] = 10 params; metal frac derived from 1-sum
- MPC pack/unpack: `pack_designation("1998 SF36")` → `"J98S36F"`, `unpack_designation("J98S36F")` → `"1998 SF36"`
- Spectral preprocessing: `from prospector.spectral.preprocessing import preprocess_all, preprocess_spectrum`
- Stage 0 pipeline: resample → normalize (550nm) → mask telluric (ground-based) → estimate SNR → assess quality → update DB
- Default grid: `DEFAULT_GRID = np.arange(0.35, 2.55, 0.005)` — 440 points at 5nm spacing
- Ground-based surveys for telluric masking: `GROUND_BASED_SURVEYS = {"MITHNEOS", "SMASS"}`
- Quality flags: 'good' (SNR≥25, coverage≥1.5μm), 'low_snr' (SNR<25), 'partial' (narrow coverage)
- Normalization falls back to nearest valid wavelength when 550nm is out of range (e.g., NIR-only spectra)
- Taxonomy classification: `from prospector.spectral.taxonomy import classify_spectrum, classify_all`
- classify_spectrum returns dict: {'class', 'prob', 'probabilities', 'prob_vector'} or None
- classify_all processes only asteroids with normalized spectra not yet in taxonomy table
- Taxonomy prob_vector: numpy BLOB of 17 floats in MAHLKE_CLASSES order (A,B,C,Ch,D,E,K,L,M,O,P,Q,R,S,V,X,Z)
- Coverage detection: 'vnir' (vis+NIR), 'vis_only', 'nir_only' based on wl_min/wl_max thresholds
- LCDB ingest: `from prospector.ingest.lcdb import ingest_lcdb, classify_spin` — takes filepath + conn, returns row count
- LCDB cross-match: IAU number first, designation fallback (same as NEOWISE pattern)
- Spin barrier: P < 2.2 hr → monolithic (above spin barrier), P > 6.0 hr + Amp > 0.7 mag → binary suspect
- Scorer spin modifier: `compute_spin_modifier(is_monolithic, is_binary_suspect)` → 1.15 bonus / 0.80 penalty / 1.0 default
- score_asteroid() accepts `is_monolithic` and `is_binary_suspect` kwargs; score_all() LEFT JOINs rotation_properties
- Mock classy in tests: `patch.dict("sys.modules", {"classy": mock_classy})` pattern — avoids tensorflow dependency in test suite
- Band analysis: `from prospector.spectral.band_analysis import analyze_all, analyze_spectrum`
- Band analysis eligibility: SILICATE_CLASSES = {S, Q, K, A, L, O, R, V} + normalized spectrum + wl_max >= 2.0 μm
- Band analysis stores results in band_analysis table: band1_center, band2_center, bar, ol_opx_ratio, fa/fs_mol_pct, gaffey_subtype, calibration
- Gaffey subtypes: S(I)-S(VII) zone classification; only S(IV) gets Dunn calibration (dunn2010), others get gaffey1993_zone
- Telluric NaN gaps handled naturally by polynomial fitting — find_band_center interpolates through gaps
- Temperature correction: T ≈ 280/sqrt(a) K; band centers shift by ~5.7e-5 (BI) and ~1.0e-4 (BII) um/K relative to 300K lab
- Granvik prior: `from prospector.scoring.granvik_prior import taxonomy_prior` — takes (a, e, i_deg), returns 17-element numpy array
- Granvik prior output is BLOB-compatible with taxonomy.prob_vector — scorer can treat both uniformly
- Scorer: `from prospector.scoring.scorer import score_all, score_asteroid` — Monte Carlo composite mining score
- score_all(conn, mode, n_samples) writes to scores table; uses COALESCE(o.diameter, pp.diameter_km) for diameter fallback
- score_asteroid returns dict with composite_score, estimated_mass_kg, grade_estimate, target_material, material_contributions
- Recoverability factors: Phase 1 defaults in scorer.py; Phase 2 will load from YAML config
- Gaia DR3 ingest: `from prospector.ingest.gaia import ingest_gaia_file, ingest_gaia_tap` — TAP query or local CSV/VOTable
- Phase curve: `from prospector.spectral.phase_curve import fit_phase_curve, fit_all, h_g1g2_model, geometric_albedo`
- Phase curve fit_all reads from user-created `mpc_photometry` table (asteroid_id, phase_angle_deg, reduced_mag, mag_unc)
- Gaia table `gaiadr3.sso_reflectance_spectrum`: one row per (source_id, wavelength) — 16 bands per asteroid, wavelength in nm
- SMASS ingest: `from prospector.ingest.smass import ingest_smass_dir, ingest_smass_file` — reuses MITHNEOS reader
- Gaia cross-match: `number_mp` → IAU number (preferred), `denomination` → entity_resolver (fallback), skip `Gaia-DR3SSO-*` internal IDs
- Gaia quality flags: 0=good, 1=poor, 2=compromised — `skip_compromised=True` filters all-compromised spectra
- RELAB ingest: `from prospector.ingest.relab import ingest_relab_dir, ingest_relab_file` — PDS4 .tab/.xml pairs
- RELAB stores in `lab_spectra` table (no asteroid FK) — meteorite samples join conceptually via meteorite_type → taxonomy class
- RELAB PDS4 XML namespace: `speclib` = `http://pds.nasa.gov/pds4/speclib/v1` — use `specimen_id`, `specimen_name`, `specimen_type`, `rock_subtype`
- RELAB tab files: wavelength in nm (convert to μm), reflectance — 2-column fixed-width ASCII
- RELAB meteorite identification: `specimen_type` contains "Meteorite" (values: "Other Meteorite", "Ordinary Chondrite", "Meteorite")
- RELAB rock_subtype has multiple values: first=group (e.g., "Carbonaceous Chondrite"), second=type (e.g., "CM2")
- Space weathering: `from prospector.spectral.weathering import deweather_all, fit_cs, deweather_spectrum`
- Brunetto model: R_obs = R_fresh × exp(Cs/λ); Cs < 0 for reddening; deweather = R_obs × exp(-Cs/λ)
- Cs fit: least-squares ln(R_norm) vs 1/λ in continuum regions (0.50-0.70, 1.25-1.35, 2.20-2.50 μm)
- Weathering columns added via `_migrate_add_columns()` in db.py — ALTER TABLE IF NOT EXISTS pattern
- Deweathered BLOB stored alongside original reflectance — band_analysis can prefer deweathered when available
- SsODNet ingest: `from prospector.ingest.ssodnet import ingest_ssodnet, ingest_ssodnet_json, parse_ssocard`
- SsODNet API: `https://ssp.imcce.fr/webservices/ssodnet/api/ssocard.php?q=<identifier>` — returns JSON with parameters.physical.{mass,density,thermal_inertia,taxonomy} and parameters.dynamical.{delta_v}
- SsODNet properties stored in `ssodnet_properties` table — mass_kg, density_kgm3, thermal_inertia, taxonomy_class/scheme/complex, delta_v_km_s
- SsODNet value extraction: `_safe_float()` handles both plain numbers and `{"value": X, "error": {...}}` dicts
- SsODNet supports both live API (`ingest_ssodnet(ids, conn)`) and pre-downloaded JSON (`ingest_ssodnet_json(path, conn)`)
- Curve matching: `from prospector.spectral.curve_match import find_analogs, match_all, match_spectrum`
- find_analogs(asteroid_id, conn, top_n=5) returns list of dicts with wmse, rho, scale_factor, taxonomy_weight
- match_spectrum(wl_ast, refl_ast, wl_lab, refl_lab) resamples to overlap, applies free scaling, computes WMSE + ρ
- Taxonomy-meteorite mapping in TAXONOMY_METEORITE_MAP: S→{OC,Iron}, C→{CC}, M→{Iron}, V→{HED}, etc.
- RHO_THRESHOLD = 0.90; constant spectra (zero std) get rho=1.0 if WMSE≈0
- CNN mineral: `from prospector.spectral.cnn_mineral import predict_all, predict_spectrum, assess_agreement`
- CNN grid: 0.45-2.45μm at 5nm (401 channels); resample via scipy.interpolate.interp1d
- PyTorch is optional dep — `_check_dependencies()` pattern for graceful fallback
- cnn_mineral table: ol/opx/cpx_pct, fa/fs/wo_mol_pct, uncertainties, classical_agreement, method
- PGM convergence: `from prospector.spectral.pgm_convergence import assess_asteroid, assess_all`
- assess_asteroid returns dict with pgm_tier, pgm_confidence, individual signals; None if not candidate
- PGM tiers: Tier 1 (M+featureless+iron+radar), Tier 2 (M+featureless+iron), Tier 3 (M only), Tier 4 (speculative X), Tier S (S-type OC metal)
- pgm_convergence table: tier, confidence, m_type_prob, featureless_nir, no_silicate_bands, iron_analog_match, s_type_metal, signal_count
- Confidence config: `scoring/confidence_config.yaml` — 8-factor weighted geometric mean, floor=0.05
- Confidence factors: wavelength_coverage(0.25), taxonomy_entropy(0.20), snr(0.12), concordance(0.12), method_agreement(0.10), n_observations(0.08), supplementary_data(0.08), pgm_convergence(0.05)
- Lambert solver: `from prospector.ephemeris.lambert import solve_lambert, search_windows, search_all, prefilter`
- solve_lambert(r1, r2, tof, mu) returns (v1, v2) or None; uses bisection on universal variable
- search_windows(asteroid_id, conn, start_jd, horizon_years) returns list[TransferWindow] sorted by dv_total
- prefilter(moid, incl, a) gates which asteroids enter the expensive grid search
- EVOI: `from prospector.scoring.evoi import compute_evoi, rank_all, compute_evoi_for_asteroid`
- compute_evoi returns dict with evoi_vnir, evoi_vis, evoi_radar, evoi_albedo, best_observation
- rank_all(conn, neo_only=True) returns list[EVOIResult] sorted by best_evoi descending
- Observation models: peaked posterior (VNIR), complex posterior (vis), Bayesian update (radar/albedo)
- Output: `from prospector.output import generate_ranked_csv, generate_ranked_dicts`
- generate_ranked_csv(conn, path, mode, limit) writes FR-8 CSV; returns count
- generate_ranked_dicts(conn, mode, limit) returns list[dict] for programmatic access

---

## 2026-02-23 — near-earth-3o3: project-setup
- Created `pyproject.toml` with numpy, scipy, pandas, astroquery as core deps
- Created `prospector/` package with 4 subpackages: ingest, spectral, scoring, ephemeris
- Created `tests/` with conftest.py (project_root, data_dir fixtures) and smoke tests
- Created `scoring/` directory for YAML config artifacts
- **Learnings for future iterations:**
  - `data/` is fully gitignored — don't try to track .gitkeep files in it
  - Tests run from project root: `python3 -m pytest tests/ -v`
  - No venv setup yet — using system Python 3.11.8
---

## 2026-02-23 — near-earth-ihi: schema-design
- Created `prospector/db.py`: full PRD §12 schema (7 tables), connection management
- Tables: asteroids, orbits, physical_properties, spectra, taxonomy, band_analysis, scores
- `get_connection()` enables WAL journal mode and foreign keys; supports `:memory:` and file paths
- `init_schema()` and `table_names()` helper functions
- 13 tests in `tests/test_db.py`: schema init, idempotency, FK enforcement, numpy BLOB round-trip, multiple spectra per asteroid, connection options
- **Learnings for future iterations:**
  - Use `get_connection(":memory:")` in tests — fast, no cleanup needed
  - Foreign keys are enforced (PRAGMA foreign_keys=ON) — insert parent rows first
  - SQLite stores booleans as 0/1 integers — test assertions should expect int, not bool
  - `CREATE TABLE IF NOT EXISTS` makes schema idempotent — downstream modules can call `init_schema()` safely
---

## 2026-02-23 — near-earth-cxl: sbdb-ingest
- Created `prospector/ingest/sbdb.py`: SBDB bulk CSV → asteroids + orbits tables
- Key functions: `ingest_sbdb_csv()`, `spkid_to_asteroid_id()`, `parse_full_name()`
- Handles: numbered/unnumbered asteroids, missing fields as NULL, neo/pha flag parsing, neo_only filter
- Uses pandas chunked reading (configurable batch_size) for large CSVs
- INSERT OR REPLACE for idempotent re-ingestion
- 36 tests in `tests/test_ingest_sbdb.py`: unit tests for helpers + integration tests with sample CSV
- **Learnings for future iterations:**
  - SBDB full_name format: `"  4179 Toutatis (1989 FB)"` — leading spaces, optional parens for designation
  - SPK-ID for numbered asteroids = 2000000 + IAU number; unnumbered use 3xxxxxx range
  - Use `pd.isna()` to check for NaN in pandas — handles float NaN, None, and pd.NA
  - Always normalize CSV column names to lowercase — SBDB exports may vary in casing
  - `_safe_float()` helper pattern: try float(), catch ValueError/TypeError, return None
---

## 2026-02-23 — near-earth-2qi: neowise-ingest
- Created `prospector/ingest/neowise.py`: NEOWISE PDS SBN diameter/albedo → physical_properties table
- Key function: `ingest_neowise()` — reads CSV or IPAC table format, cross-matches to existing asteroids
- Supports flexible column name resolution with aliases (handles varying PDS SBN column names)
- Cross-match: IAU number (preferred, checked against preloaded known_ids set) → designation fallback (DB lookup)
- FK-safe: preloads known asteroid_ids to avoid IntegrityError on child table inserts
- 32 tests in `tests/test_ingest_neowise.py`: unit tests for helpers + integration tests with sample data
- **Learnings for future iterations:**
  - Preload `known_ids` set before batch insertion into FK-constrained child tables — avoids IntegrityError
  - `_resolve_columns()` pattern: map logical names → actual DF columns via explicit map + alias fallback — reusable for any variable-format input
  - NEOWISE PDS SBN uses IPAC table format (.tab) — `astropy.io.ascii.read(path, format='ipac')` handles it
  - For cross-matching without entity-resolver: number match covers >95% of cases; designation fallback covers the rest
  - Test fixtures: create `db_with_asteroids` fixture that pre-populates asteroids table for downstream ingest tests
---

## 2026-02-23 — near-earth-d52: validate-classy
- Research spike: verified `space-classy` 0.8.8 installs and works on Python 3.12
- Mahlke 2022 MCFA taxonomy produces probabilistic 17-class assignments
- Full API documented in `tasks/validate-classy-findings.md`
- 17 tests in `tests/test_validate_classy.py`: installation, spectrum creation, Mahlke classification, DeMeo fallback, preprocessing
- **Learnings for future iterations:**
  - PyPI name is `space-classy`, import name is `classy` — NOT the cosmology `classy` package
  - Albedo kwarg MUST be `pV=value`, not `albedo=value` — `albedo=` is silently ignored
  - Requires tensorflow 2.16 + tensorflow-probability==0.24.* + tf-keras — version mismatches break MCFA model loading
  - space-classy pins numpy<2.0 — conflicts with packages needing numpy>=2; use dedicated venv
  - `ROCKS_CACHE_DIR` env var avoids interactive cache dir prompt on first run
  - MCFA model handles missing values (NaN) via imputation — partial wavelength coverage works
  - DeMeo taxonomy (PCA-based) is available as cross-reference but not probabilistic
  - Classify from /tmp or non-numpy-source directory to avoid numpy import issues
  - Wrapper pattern for taxonomy.py: extract probabilities via `getattr(spec, f'class_{c}')` for all 17 classes
---

## 2026-02-23 — near-earth-mpo: entity-resolver
- Created `prospector/ingest/entity_resolver.py`: cross-survey ID normalization
- Key functions: `pack_designation()`, `unpack_designation()`, `normalize_designation()`, `resolve()`, `parse_mithneos_filename()`
- MPC packed↔unpacked conversion with full cycle encoding (0-619)
- DB-backed resolution: number → SPK-ID → packed designation → unpacked designation → name → alias (full_name search)
- Pure conversion mode (no DB) for designation transforms
- ResolvedID dataclass with asteroid_id, designation, packed, source
- 230 tests in `tests/test_entity_resolver.py`: 50+ known multi-survey asteroids, cycle encoding/decoding, pack/unpack roundtrips, DB resolution
- **Learnings for future iterations:**
  - MPC packed designation format: [century][yy][half_month][cycle_2char][second_letter] — 7 chars total
  - Century codes: I=18xx, J=19xx, K=20xx
  - Cycle encoding: 0-99 as two digits; 100+ as letter+digit (A=10..Z=35, a=36..z=61 for tens place)
  - MPC half-month letters skip I (A-H, J-Y); order letters skip I (A-H, J-Z)
  - `from prospector.ingest.entity_resolver import resolve, pack_designation, unpack_designation`
  - `resolve(identifier, conn)` returns `ResolvedID` or None — works with any identifier format
  - Downstream ingest modules (MITHNEOS, SMASS, Gaia) should use `resolve()` for cross-matching
---

## 2026-02-23 — near-earth-4w9: mithneos-ingest
- Created `prospector/ingest/mithneos.py`: MITHNEOS ASCII spectra → spectra table
- Key functions: `_read_spectrum_file()` (2- or 3-column ASCII), `ingest_mithneos_file()` (single file), `ingest_mithneos_dir()` (batch)
- Filename → asteroid_id via `parse_mithneos_filename()` from entity_resolver (numbered: `a004179.sp05.txt` → 4179)
- Unnumbered objects resolved via `resolve()` with DB lookup
- FK validation: preloads `known_ids` set (same pattern as NEOWISE ingest)
- Stores wavelengths/reflectance/uncertainty as numpy BLOBs; records wl_min, wl_max, survey='MITHNEOS'
- Supports multiple spectra per asteroid (different .spNN observations)
- 19 tests in `tests/test_ingest_mithneos.py`: file parsing, BLOB round-trip, FK integrity, batch ingest
- **Learnings for future iterations:**
  - MITHNEOS files are simple whitespace-delimited: wl(μm), reflectance, optional uncertainty — no header
  - `parse_mithneos_filename()` already exists in entity_resolver — don't reimplement
  - Spectra table uses AUTOINCREMENT on spectrum_id — INSERT (not INSERT OR REPLACE) for multiple spectra per asteroid
  - Pattern: preload known_ids set for FK validation before batch inserts into child tables
  - Future spectral ingest modules (SMASS, Gaia) can follow same structure: read → resolve → FK check → BLOB store
---

## 2026-02-23 — near-earth-d3l: spectral-preprocessing
- Created `prospector/spectral/preprocessing.py`: Stage 0 normalization and QC
- Key functions: `resample()`, `normalize()`, `mask_telluric()`, `estimate_snr()`, `assess_quality()`, `preprocess_spectrum()`, `preprocess_all()`
- Resamples to common 5nm grid (0.35-2.55μm), NaN outside original range
- Normalizes reflectance=1.0 at 550nm (falls back to nearest valid wavelength for NIR-only)
- Masks telluric absorption (1.35-1.45μm, 1.80-2.00μm) for ground-based surveys only
- SNR estimation: uncertainty-based (median |refl|/unc) or smoothness heuristic (running-median residuals)
- Quality flagging: 'good' (SNR≥25, coverage≥1.5μm), 'low_snr', 'partial'
- Updates spectra table in-place: sets normalized=TRUE, snr_estimate, quality_flag, replaces BLOBs with resampled data
- 39 tests in `tests/test_spectral_preprocessing.py`: unit tests per function + integration tests on DB
- **Learnings for future iterations:**
  - `preprocess_all(conn)` only processes rows with `normalized=FALSE OR normalized IS NULL` — safe to re-run
  - Telluric masking sets reflectance to NaN (not removal) — downstream must handle NaN channels
  - SNR smoothness heuristic uses window=7 running median — works well for typical 200+ channel spectra
  - Space-based surveys (Gaia) skip telluric masking — add to `GROUND_BASED_SURVEYS` set for new ground surveys
  - wl_min/wl_max after preprocessing reflect valid (non-NaN) range, not full grid extent
  - `preprocess_spectrum()` copies numpy arrays from `frombuffer()` before modification (avoids read-only buffer errors)
---

## 2026-02-23 — near-earth-8pd: taxonomy-module
- Created `prospector/spectral/taxonomy.py`: Stage 1 Mahlke 2022 classification wrapper
- Key functions: `classify_spectrum()` (core classy wrapper), `classify_asteroid()` (DB integration), `classify_all()` (batch)
- `determine_coverage()` classifies input as vnir/vis_only/nir_only from wavelength bounds
- Filters NaN channels before passing to classy (handles telluric-masked spectra)
- Selects best spectrum per asteroid: good quality first, widest coverage as tiebreaker
- Looks up NEOWISE albedo from physical_properties table, passes as pV= kwarg
- Stores in taxonomy table: primary_class, primary_prob, prob_vector (numpy BLOB), classifier, input_coverage
- INSERT OR REPLACE for idempotent re-runs; classify_all skips already-classified asteroids
- 29 tests in `tests/test_spectral_taxonomy.py`: mocked classy to avoid tensorflow dependency
- **Learnings for future iterations:**
  - Mock classy via `patch.dict("sys.modules", {"classy": mock_module})` — clean, no tensorflow import
  - classy.Spectrum results accessed via `spec.class_mahlke` (str), `spec.prob` (float), `spec.class_X` (per-class float)
  - prob_vector stored as 17-element numpy float64 array in MAHLKE_CLASSES order — reconstruct dict via `zip(MAHLKE_CLASSES, np.frombuffer(blob))`
  - Coverage thresholds: VIS_UPPER=0.8μm, NIR_LOWER=1.0μm — boundary cases tested
  - `classify_all` catches per-asteroid exceptions so one failure doesn't block the batch
---

## 2026-02-23 — near-earth-8yl: band-analysis
- Created `prospector/spectral/band_analysis.py`: Stage 2 classical band parameter extraction
- Key functions: `find_peak()`, `linear_continuum()`, `remove_continuum()`, `find_band_center()`, `compute_band_area()`, `analyze_spectrum()`, `analyze_asteroid()`, `analyze_all()`
- Continuum removal: linear continua tangent to peaks at ~0.7μm/~1.5μm (Band I) and ~1.5μm/~2.4μm (Band II)
- Band centers via degree-4 polynomial fit + scipy.optimize.minimize_scalar
- Temperature correction (Sanchez 2012): T=280/sqrt(a), shifts band centers by dBIC/dT and dBIIC/dT
- BAR = BII_area/BI_area with Lindsay (2015/2016) red-edge correction
- Dunn et al. (2010) calibrations for S(IV): ol/(ol+px), Fa, Fs
- Gaffey S(I)-S(VII) zone classification from BIC vs BAR
- Only processes silicate-class asteroids {S,Q,K,A,L,O,R,V} with NIR coverage ≥ 2.0μm
- 51 tests in `tests/test_spectral_band_analysis.py`: unit + integration + DB tests
- **Learnings for future iterations:**
  - Telluric NaN gaps (1.35-1.45, 1.80-2.00 μm) are handled by polynomial fitting — no special gap-bridging needed
  - Band II minimum (~1.95 μm) often falls in telluric mask — polynomial interpolates through the gap
  - PRD Fa/Fs linear formulas give low values compared to literature; ol/(ol+px) from BAR is the most reliable Dunn output
  - scipy.optimize.minimize_scalar with method='bounded' works well for polynomial minima in bounded wavelength ranges
  - analyze_all follows same pattern as classify_all: query eligible, try each, catch exceptions, commit at end
  - Synthetic Gaussian absorption bands make good test spectra — controllable center, depth, width
---

## 2026-02-23 — near-earth-f4q: scoring-config
- Created `scoring/grade_density_priors.yaml`: literature-derived distributions for Monte Carlo scoring
- Density priors: all 17 Mahlke classes from Carry (2012) bulk densities (includes macroporosity)
- Grade estimates: PGM (Cannon 2023), water (Alexander 2012), iron/olivine/pyroxene (Dunn/Hutchison/Burbine)
- S-type subgroup metal fractions (H/L/LL) for extraction cost modeling
- All values as {mean, std, min, max} distributions with source citations and notes
- 23 tests in `tests/test_scoring_config.py`: structure, distributions, physical plausibility, Psyche mass check
- **Learnings for future iterations:**
  - YAML config loading: `yaml.safe_load(open(path))` — all values are plain Python types (float, str, dict)
  - Scorer should load config once and look up by taxonomy class + material key
  - Porosity decision: use Carry (2012) bulk densities for mass estimation (correct for diameter-based volume)
  - PGM Kargel→Cannon correction: ~60% reduction in estimated PGM grades for M-type
  - X-type is degenerate without albedo — always use wide priors for X and Z classes
  - S-type PGM requires separate metal fraction lookup for extraction cost modeling
---

## 2026-02-23 — near-earth-0s8: granvik-prior
- Created `prospector/scoring/granvik_prior.py`: Granvik 2018 debiased population prior
- Key functions: `source_region_probabilities(a, e, i_deg)`, `taxonomy_prior(a, e, i_deg)`, `taxonomy_prior_dict(a, e, i_deg)`
- 7 source regions: ν6, 3:1, 5:2, 2:1, Hungaria, Phocaea, JFC
- Gaussian proximity kernels in (a,e,i) space weighted by Granvik 2018 steady-state delivery rates
- Tisserand parameter boost for JFC identification (2 < T_J < 3)
- Source → taxonomy mapping from DeMeo & Carry (2014) compositional gradient
- Returns 17-element Mahlke probability vector (same format as taxonomy.prob_vector BLOB)
- 40 tests in `tests/test_scoring_granvik_prior.py`: Tisserand, source regions, taxonomy vectors, known NEOs, BLOB interop
- **Learnings for future iterations:**
  - `taxonomy_prior(a, e, i_deg)` returns numpy float64 array compatible with taxonomy.prob_vector BLOB format
  - Scorer should call `taxonomy_prior()` for each asteroid — no new DB table needed
  - Tisserand for JFC: use 67P-like orbit (a≈3.46, e≈0.64) for testing, not Encke (T_J barely above 3)
  - Source taxonomy vectors are pre-normalised at module load time via `_build_source_vectors()`
  - Extreme orbits (far from all resonances) fall back to uniform prior over all 7 regions
  - `from prospector.scoring.granvik_prior import taxonomy_prior, source_region_probabilities`
---

## 2026-02-23 — near-earth-4mt: scorer
- Created `prospector/scoring/scorer.py`: Monte Carlo composite mining scorer
- Key functions: `score_asteroid()` (single asteroid MC scoring), `score_all()` (batch DB integration)
- Helper functions: `sample_from_dist()`, `estimate_mass_kg()`, `compute_accessibility()`, `compute_confidence()`
- MC loop: draw taxonomy class from prob_vector → sample density → compute mass → sum material values × recoverability × unit_value → apply confidence × accessibility
- Two economic modes: `earth_return` (PGMs dominate) and `in_space` (water/structural dominate)
- Accessibility proxy: exponential decay on MOID (exp(-moid/0.1)); fallback to perihelion distance
- Confidence: 1 − H/H_max where H is Shannon entropy of taxonomy prob_vector
- Phase 1 recoverability factors as constants (full design deferred to Phase 2 bead)
- Material values configurable via YAML or dict override; defaults for 5 materials
- score_all queries asteroids+orbits+physical_properties+taxonomy, uses COALESCE for diameter fallback
- 55 tests in `tests/test_scoring_scorer.py`: unit tests, MC scoring, DB integration, physical sanity checks
- **Learnings for future iterations:**
  - `from prospector.scoring.scorer import score_all, score_asteroid` — main entry points
  - `score_asteroid()` returns dict with composite_score, estimated_mass_kg, grade_estimate, target_material, material_contributions, etc.
  - `score_all(conn, mode='earth_return', n_samples=1000)` — writes to scores table, returns count
  - Use `rng=np.random.default_rng(seed)` for deterministic testing of MC scorer
  - `_grade_to_fraction()` converts ppm→1e-6, wt_pct→/100 — downstream beads should use same convention
  - Material contributions dict shows per-material value breakdown — useful for output-csv bead
  - Phase 1 recoverability factors are hardcoded defaults — recoverability-accessibility bead will replace with YAML-driven values
  - COALESCE(o.diameter, pp.diameter_km) in SQL picks SBDB diameter first, NEOWISE fallback
---

## 2026-02-23 — near-earth-8dm: gaia-dr3-ingest
- Created `prospector/ingest/gaia.py`: Gaia DR3 reflectance spectra → spectra table
- Key functions: `ingest_gaia_tap()` (TAP query), `ingest_gaia_file()` (local CSV/VOTable), `_ingest_dataframe()` (core logic)
- Helper functions: `_query_gaia_tap()`, `_load_gaia_file()`, `_group_spectra()`, `_resolve_asteroid_id()`, `_has_good_bands()`
- Per-wavelength rows grouped by source_id → 16-band numpy arrays (wavelengths in nm → μm conversion)
- Cross-match: `number_mp` → IAU number (direct), `denomination` → entity_resolver (fallback)
- Quality gate: skip spectra where all 16 bands have `reflectance_spectrum_flag == 2` (compromised)
- FK validation: preloads known_ids set (same pattern as MITHNEOS/NEOWISE)
- Stores as numpy BLOBs with survey='Gaia', wl_min≈0.374μm, wl_max≈1.034μm
- 32 tests in `tests/test_ingest_gaia.py`: grouping, quality flags, resolution, DB integration, CSV file, sanity checks
- **Learnings for future iterations:**
  - Gaia DR3 `sso_reflectance_spectrum` stores one row per (source_id, wavelength) — must GROUP BY to get per-asteroid spectra
  - Wavelengths in nm in Gaia archive — convert to μm for our schema consistency
  - `number_mp` is the Gaia equivalent of IAU asteroid number — direct match when present
  - `denomination` field needs whitespace normalization; `Gaia-DR3SSO-*` prefixed entries are internal IDs (skip)
  - Quality flags (0/1/2) allow selective filtering — skip_compromised=True is the safe default
  - astroquery.gaia: use `Gaia.launch_job_async()` for the full ~968K row dataset
  - SMASS ingest can follow same pattern: load → group → resolve → FK check → BLOB store
---

## 2026-02-23 — near-earth-q2y: smass-ingest
- Created `prospector/ingest/smass.py`: SMASS II visible spectra → spectra table
- Key functions: `ingest_smass_file()` (single file), `ingest_smass_dir()` (batch)
- Reuses `_read_spectrum_file` from MITHNEOS (same ASCII format: wl, refl, optional unc)
- Reuses `parse_mithneos_filename` from entity_resolver (PRD confirms "similar naming convention")
- Stores as numpy BLOBs with survey='SMASS', wl_min≈0.44μm, wl_max≈0.92μm
- FK validation via preloaded known_ids set (same pattern as all other ingest modules)
- 15 tests in `tests/test_ingest_smass.py`: single file, batch, BLOB roundtrip, FK integrity, MITHNEOS coexistence
- **Learnings for future iterations:**
  - SMASS file format is identical to MITHNEOS — direct reuse of `_read_spectrum_file` works
  - `parse_mithneos_filename` is survey-agnostic (just parses `aXXXXXX.spNN.txt` pattern) — could be renamed
  - SMASS is already in `GROUND_BASED_SURVEYS` set in preprocessing.py — telluric masking will apply
  - `from prospector.ingest.smass import ingest_smass_dir, ingest_smass_file`
---

## 2026-02-23 — near-earth-cmy: relab-ingest
- Created `prospector/ingest/relab.py`: RELAB PDS4 meteorite spectral library → lab_spectra table
- Key functions: `_parse_tab_file()` (2-col ASCII, nm→μm), `_parse_xml_label()` (PDS4 XML metadata), `ingest_relab_file()`, `ingest_relab_dir()`
- Helper: `is_meteorite()` filters by `specimen_type` containing "Meteorite"
- Added `lab_spectra` table to `prospector/db.py` — no FK to asteroids (meteorite samples are independent)
- Table columns: sample_id, spectrum_key (PK), meteorite_name, meteorite_type, meteorite_group, sample_desc, grain_size_min/max, wavelengths/reflectance BLOBs, wl_min/max, source
- Indexes on meteorite_type, meteorite_group, sample_id for query performance
- INSERT OR REPLACE keyed on spectrum_key (filename stem) for idempotent re-ingestion
- 35 tests in `tests/test_ingest_relab.py`: tab parsing, XML parsing, is_meteorite, file/dir ingest, BLOB roundtrip, schema integration, iron meteorite PGM use case
- Updated EXPECTED_TABLES in `tests/test_db.py` to include `lab_spectra`
- **Learnings for future iterations:**
  - RELAB PDS4 archive at `pds-geosciences.wustl.edu/speclib/urn-nasa-pds-relab/` has `data_reflectance/bdr2/`, `bdr3/`, `ftir1/`, `ftir2/` subdirs
  - Each spectrum is a paired `.tab` + `.xml` file; spectrum_key = filename stem (unique)
  - PDS4 XML uses `speclib:` namespace prefix — parse with `xml.etree.ElementTree` + namespace dict
  - `rock_subtype` can have MULTIPLE XML elements: first=broad group, second=specific classification (e.g., "CM2")
  - Grain size in `specimen_min_size`/`specimen_max_size` with `unit="micrometer"` attribute — only present for particulate samples
  - ~23,606 total RELAB spectra; filtering for meteorites reduces to a subset needed for analog matching
  - No asteroid entity resolution needed — curve-matching bead will join via meteorite_type → taxonomy class
  - `from prospector.ingest.relab import ingest_relab_dir, ingest_relab_file`
---

## 2026-02-23 — near-earth-muo: space-weathering-correction
- Created `prospector/spectral/weathering.py`: Stage 1B Brunetto et al. (2006) space weathering correction
- Key functions: `fit_cs()` (Cs parameter from continuum slope), `deweather_spectrum()` (apply correction), `deweather_asteroid()` (DB integration), `deweather_all()` (batch)
- Brunetto model: R_observed(λ) = R_fresh(λ) × exp(Cs/λ); Cs < 0 for reddening/darkening
- Cs fitting: least-squares regression of ln(R_norm) vs 1/λ in three continuum regions (0.50-0.70, 1.25-1.35, 2.20-2.50 μm)
- Sanity bounds: Cs ∈ [-2.0, 0.1] μm; insufficient continuum points returns None
- Only applies to silicate classes: {S, Q, K, A, L, O, R, V}
- Added `weathering_cs` (REAL) and `deweathered` (BLOB) columns to spectra table via schema migration
- Migration: `_add_column_if_missing()` pattern in db.py — uses PRAGMA table_info to check, ALTER TABLE to add
- Preserves original reflectance — deweathered stored separately for downstream use
- 31 tests in `tests/test_spectral_weathering.py`: Cs fitting, roundtrip recovery, DB integration, batch, schema migration, physical sanity
- **Learnings for future iterations:**
  - Schema migration pattern: `_add_column_if_missing(conn, table, column, type)` — safe for re-running
  - Brunetto Cs typically -0.5 to 0.0 for real asteroids; -0.15 is moderate weathering
  - Absorption bands don't interfere with Cs fitting because we use continuum-only regions
  - band_analysis should check for `deweathered` column and prefer it when available
  - `from prospector.spectral.weathering import deweather_all, fit_cs, deweather_spectrum`
---

## 2026-02-23 — near-earth-n40: curve-matching
- Created `prospector/spectral/curve_match.py`: Stage 3 meteorite analog curve matching
- Key functions: `match_spectrum()`, `compute_match_score()`, `find_analogs()`, `match_all()`
- Helpers: `_resample_to_overlap()`, `_best_scale_factor()`, `_taxonomy_weight()`
- Method: resample to overlap grid → free scaling α=(ast·lab)/(lab·lab) → WMSE + correlation ρ
- Rank by WMSE ascending, reject ρ < 0.90 (PRD Option A)
- Taxonomy-consistency filter: prob_vector weighted by TAXONOMY_METEORITE_MAP compatibility
- Prefers deweathered spectrum when available (checks spectra.deweathered column)
- Returns top-N analogs as list of dicts (no new DB table — pure computation)
- 35 tests in `tests/test_spectral_curve_match.py`: resampling, scaling, scoring, taxonomy weights, DB integration, physical sanity
- **Learnings for future iterations:**
  - Free scaling handles albedo mismatch: α minimizes ||ast - α*lab||² analytically
  - Constant spectra (featureless M-type matching featureless iron) need special rho handling: rho=1.0 if WMSE≈0
  - RELAB spectra may have different wavelength grids → resample via scipy.interpolate.interp1d
  - MIN_OVERLAP_POINTS=20 ensures statistical significance in match metrics
  - `_taxonomy_weight()` is probabilistic: partial C-probability allows surprising CC matches for ambiguous X-type
  - `from prospector.spectral.curve_match import find_analogs, match_all, match_spectrum`
---

## 2026-02-23 — near-earth-1ct: recoverability-accessibility
- Created `scoring/recoverability_accessibility.yaml`: recoverability factors + accessibility function design
- Recoverability: 5 materials × taxonomy classes, values 0-1 with literature rationale
  - PGMs: M=0.30 (bulk metal), S=0.05 (two-stage OC extraction), per Cannon & Britt 2019
  - Water: C/Ch=0.70 (thermal extraction), S=0.00 (450 μg/g below threshold)
  - Iron: M=0.80 (bulk), S=0.10 (OC metal grains)
  - Olivine: A=0.80 (olivine-dominated), S=0.40 (OC silicate fraction)
  - Pyroxene: V=0.60 (HED), E=0.50 (enstatite)
- Accessibility: exponential decay exp(-dv/dv_scale), dv_scale=7.0 km/s
  - ERO (<0.5 km/s) → ~0.93; NHATS (12 km/s) → ~0.18; Hard (>15 km/s) → <0.12
  - MOID fallback: exp(-moid/0.1) consistent with Phase 1
  - Frequency multiplier: min(1.0, sqrt(1/synodic_years))
- 4 accessibility tiers: ERO, Easy, Feasible, Difficult
- 5 verification examples: Psyche, Bennu, Itokawa, Apophis, Anteros
- 30 tests in `tests/test_scoring_recoverability.py`
- **Learnings for future iterations:**
  - Scorer should load this YAML and replace DEFAULT_RECOVERABILITY hardcoded dict
  - Accessibility function when Lambert solver available: use dv_total with dv_scale=7.0
  - Frequency multiplier is separate from delta-v score — apply as a product
  - Garcia Yarnoz 2013 (NOT Elvis 2014) for ERO attribution — per compass audit
---

## 2026-02-23 — near-earth-xuk: cnn-mineral-quantification
- Created `prospector/spectral/cnn_mineral.py`: Stage 2B CNN-based mineral quantification wrapper
- Key functions: `resample_to_cnn_grid()`, `predict_spectrum()`, `predict_asteroid()`, `predict_all()`, `assess_agreement()`
- Helper functions: `_fill_nan_channels()`, `_check_dependencies()`, `_load_model()`
- CNN input: VNIR spectrum resampled to 0.45-2.45μm at 5nm spacing (401 channels)
- Domain check: S*-complex only (same SILICATE_CLASSES as band_analysis/weathering)
- NaN gap filling via linear interpolation for CNN's continuous input requirement
- Modal abundance normalization: OL+OPX+CPX → 100%
- MC dropout uncertainty: n_mc forward passes with dropout enabled
- Agreement assessment: compares CNN ol/(ol+opx) with classical Dunn ol_opx_ratio
- Added `cnn_mineral` table to schema in `db.py` (14 columns + method identifier)
- Updated EXPECTED_TABLES in `tests/test_db.py` to include `cnn_mineral`
- PyTorch and Asteroid-spectra are optional dependencies (graceful ImportError)
- 44 tests in `tests/test_spectral_cnn_mineral.py` (4 skipped without PyTorch)
- **Learnings for future iterations:**
  - PyTorch optional dependency pattern: `_check_dependencies()` + late import inside functions
  - CNN expects continuous input — NaN gaps from telluric masking must be interpolated through
  - scipy.interpolate.interp1d with bounds_error=False, fill_value=np.nan handles edge NaN correctly
  - Modal abundances from CNN may not sum to 100% — normalize after clipping to [0, 100]
  - `from prospector.spectral.cnn_mineral import predict_all, predict_spectrum, assess_agreement`
  - Agreement thresholds: 10pp (good), 20pp (fair) based on ~10% Dunn calibration RMSE
  - predict_all loads model once for batch efficiency via _model parameter passthrough
  - confidence-formula bead (near-earth-f4d) is now unblocked by this bead
---

## 2026-02-23 — near-earth-fd2: pgm-convergence
- Created `prospector/spectral/pgm_convergence.py`: Stage 5 multi-signal PGM candidate identification
- Key functions: `assess_nir_slope()`, `check_silicate_bands()`, `check_iron_analog()`, `assess_asteroid()`, `assess_all()`
- 7 signals assessed: M-type prob, featureless NIR, silicate band absence, iron analog match, radar albedo, beaming eta, S-type metal fraction
- 5 confidence tiers per PRD §4 Stage 5: Tier 1 (High, 70-90%), Tier 2 (Medium, 40-60%), Tier 3 (Low, 20-30%), Tier 4 (Speculative, <10%), Tier S (S-type metal)
- Added `pgm_convergence` table to `prospector/db.py` schema (14 columns)
- Updated EXPECTED_TABLES in `tests/test_db.py`
- 49 tests in `tests/test_spectral_pgm_convergence.py`: prob helpers, NIR slope, silicate bands, iron analogs, tier assignment, DB integration, batch, physical sanity
- **Learnings for future iterations:**
  - `from prospector.spectral.pgm_convergence import assess_asteroid, assess_all`
  - `assess_asteroid(id, conn, analogs=optional)` returns dict with tier + signals or None if not a PGM candidate
  - Featureless NIR: linear fit residual std < 3% of mean reflectance AND positive slope
  - Iron analog check uses `find_analogs()` from curve_match — can be slow; pass pre-computed analogs to avoid
  - Tier S pathway: S/Q-type with S(IV) Gaffey subtype OR CNN showing <95% silicate total → OC metal ~10-20%
  - Early exit optimization: skip asteroids with metallic_prob < 0.05 AND s_prob < 0.5
  - Radar albedo not yet ingested (always NULL) — Tier 1 requires radar, so currently max is Tier 2
  - PGM convergence table stores individual signal values for audit trail
---

## 2026-02-23 — near-earth-f4d: confidence-formula
- Created `scoring/confidence_config.yaml`: 8-factor Bayesian posterior confidence scoring design
- Formula: weighted geometric mean with floor=0.05 preventing zero-collapse
- 8 factors with weights summing to 1.0:
  1. Wavelength coverage (0.25) — dominant factor, lookup table for 8 coverage levels
  2. Taxonomy entropy (0.20) — Shannon entropy of 17-class prob vector, normalized by H_max=log2(17)
  3. SNR (0.12) — min(1, SNR/50), saturates at SNR=50
  4. Taxonomy-analog concordance (0.12) — full concordance map for all 17 Mahlke classes
  5. Method agreement (0.10) — classical vs CNN ol/(ol+opx) comparison
  6. N observations (0.08) — min(1, N/3), saturates at 3 independent observations
  7. Supplementary data (0.08) — weighted count of albedo/radar/thermal/polarimetry/lightcurve
  8. PGM convergence (0.05) — Stage 5 signal_count/max_signals, neutral for non-PGM
- Worked examples: Itokawa (~0.82) > Bennu (~0.81) > Eros (~0.77) > Psyche (~0.68) > Gaia-only (~0.31) > Uncharacterized (~0.10)
- 38 tests in `tests/test_scoring_confidence.py`: structure, weights, normalization, concordance, worked examples, formula properties, defaults
- **Learnings for future iterations:**
  - Confidence config is at `scoring/confidence_config.yaml` — load with yaml.safe_load()
  - Scorer implementation should: load config → compute each factor → geometric mean with floor
  - For non-PGM asteroids, pgm_convergence defaults to 0.5 (neutral)
  - Concordance map mirrors TAXONOMY_METEORITE_MAP from curve_match.py
  - `default_when_missing` field on each factor handles graceful degradation
---

## 2026-02-23 — near-earth-rvz: lambert-solver
- Created `prospector/ephemeris/lambert.py`: Lambert transfer solver + approach window search
- Key functions: `solve_lambert()`, `search_windows()`, `search_all()`, `departure_dv()`, `arrival_dv()`, `prefilter()`
- Helpers: `stumpff_c()`, `stumpff_s()`, `solve_kepler()`, `keplerian_to_cartesian()`, `earth_state()`, `asteroid_state()`
- Universal variable Lambert solver with bisection root-finding (more robust than Newton-Raphson)
- Stumpff functions with overflow protection for large negative z (hyperbolic orbits)
- Departure Δv: Oberth manoeuvre from LEO (parking orbit at 400 km altitude)
- Arrival Δv: hyperbolic excess velocity magnitude (small body, no orbit insertion)
- Grid search: launch dates × TOF combinations, keeps top-N windows by total Δv
- Prefilter: MOID < 0.15 AU, inclination < 40°, semi-major axis < 4.5 AU
- TransferWindow dataclass: launch_jd, arrival_jd, tof_days, dv_departure/arrival/total_km_s
- 47 tests in `tests/test_ephemeris_lambert.py`: Stumpff, Kepler, keplerian↔cartesian, Earth state, Lambert solver, delta-v, prefilter, synodic period, search windows, physical sanity
- **Learnings for future iterations:**
  - Near-180° Lambert transfers are degenerate (transfer plane undefined) — avoid in tests
  - Bisection root-finding is more robust than Newton-Raphson for Lambert's universal variable equation
  - Stumpff functions: cosh/sinh overflow for z < -500 — return np.inf as sentinel
  - Earth J2000 elements hardcoded for two-body propagation (sufficient for transfer window search)
  - `from prospector.ephemeris.lambert import solve_lambert, search_windows, search_all, prefilter`
  - Synodic period: |1/(1 - 1/P_ast)| years — search horizon should span at least one synodic period
  - `search_all(conn)` prefilters from orbits table, then grid-searches each candidate
---

## 2026-02-23 — near-earth-u5d: evoi
- Created `prospector/scoring/evoi.py`: Expected Value of Information ranking
- Key functions: `compute_evoi()`, `compute_evoi_for_asteroid()`, `rank_all()`
- Helpers: `_make_peaked_posterior()`, `_make_complex_posterior()`, `_make_radar_posterior()`, `_make_albedo_posterior()`
- Preposterior analysis: current σ_prior − E[σ_post | observation]
- 4 observation models: VNIR spectroscopy (peaked class), visible (complex-level), radar (metallic binary), albedo (X-degeneracy)
- EVOIResult dataclass with per-type EVOI values and best_observation recommendation
- rank_all targets uncharacterised NEOs (no spectral taxonomy) — prioritises Granvik-prior-only asteroids
- 46 tests in `tests/test_scoring_evoi.py`: posterior construction, score distribution, EVOI computation, DB integration, physical sanity
- **Learnings for future iterations:**
  - MC statistical tests need large n_samples (500+) and coefficient of variation, not raw std
  - EVOI max(0, ...) clamp prevents negative values from MC noise
  - Peaked prior EVOI near zero is expected — already well-characterised asteroids don't benefit from more observations
  - `from prospector.scoring.evoi import compute_evoi, rank_all, compute_evoi_for_asteroid`
---

## 2026-02-23 — near-earth-bv8: output-csv
- Created `prospector/output.py`: FR-8 ranked candidate list output
- Key functions: `generate_ranked_csv()`, `generate_ranked_dicts()`, `_fetch_candidates()`
- 30+ CSV columns: rank, asteroid identity, orbital elements, taxonomy, analog match, classical composition, CNN composition, PGM tier, all scoring components, transfer windows
- `_fetch_candidates()` joins scores + asteroids + orbits + physical_properties + taxonomy + band_analysis + cnn_mineral + pgm_convergence
- Optional slow features: `include_analogs=True` (calls curve_match.find_analogs), `include_windows=True` (calls lambert.search_windows)
- `generate_ranked_dicts()` provides programmatic access without file I/O
- 32 tests in `tests/test_output.py`: format helpers, fetch candidates, CSV generation, ranked dicts, FR-8 validation
- **Learnings for future iterations:**
  - `from prospector.output import generate_ranked_csv, generate_ranked_dicts`
  - CSV uses csv.DictWriter with explicit COLUMNS list for stable column order
  - _fmt and _fmt_sci helpers handle None → empty string gracefully
  - Analog and window computation are opt-in (slow for large datasets)
---

## 2026-02-23 — near-earth-h36: hapke-modeling
- Implemented Stage 3B Hapke radiative transfer forward modeling in `prospector/spectral/hapke_model.py`
- Full Hapke (1981, 2012) physics: SSA from optical constants, intimate mixture mixing, SMFe space weathering (Hapke 2001), bidirectional reflectance via H-function
- 5 built-in mineral endmembers: olivine, pyroxene, plagioclase, troilite, Fe-Ni metal — each with representative optical constants (n, k with Gaussian absorption bands)
- L-BFGS-B optimization with free scaling factor and bootstrap uncertainty estimation
- 10-parameter fit: 4 area fractions (5th derived), 5 grain sizes, 1 SMFe volume fraction
- DB integration: `model_asteroid()` reads best normalised spectrum, fits, stores to `hapke_modeling` table; `model_all()` runs batch for top-N scoring candidates
- Added `hapke_modeling` table to `prospector/db.py` schema (20 columns)
- Updated `tests/test_db.py` EXPECTED_TABLES list
- Files changed: `prospector/spectral/hapke_model.py` (new), `tests/test_spectral_hapke_model.py` (new), `prospector/db.py`, `tests/test_db.py`
- 45 tests in `tests/test_spectral_hapke_model.py`: SSA computation, mixing, SMFe weathering, reflectance conversion, forward model, fitting, DB integration, physical sanity checks
- **Learnings for future iterations:**
  - Metal (high k) is DARKER than silicates in Hapke model — opaque materials SSA ≈ Fresnel Se (~0.5), transparent silicates SSA → 1.0
  - `from prospector.spectral.hapke_model import model_asteroid, model_all, forward_model, fit_spectrum`
  - Endmember optical constants parameterized as k(λ) = baseline + Σ Gaussian bands — extensible to new minerals
  - Forward model uses free scaling factor (same as curve_match) so normalization differences don't matter
  - Bootstrap uncertainty (default 50 iterations) warm-starts from best fit — typically takes ~6s per asteroid
  - `apply_smfe()` uses Hapke (2001) formulation with characteristic rind thickness of 0.1 μm
---

## 2026-02-23 — near-earth-lvc: phase-curve-analysis
- Implemented H, G1, G2 photometric phase curve model (Muinonen et al. 2010)
- Files changed: `prospector/db.py` (added phase_curve table), `prospector/spectral/phase_curve.py` (new), `tests/test_phase_curve.py` (new, 36 tests), `tests/test_db.py` (updated table list)
- Module provides: basis functions (cubic spline on tabulated values), forward model, grid-search + L-BFGS-B fitting, taxonomy hint from G1-G2 clustering, geometric albedo, phase integral
- **Learnings for future iterations:**
  - G1 vs G2 discrimination requires opposition surge data (α < 5°) — without small phase angles, the parameters are individually degenerate
  - Two-stage fitting (magnitude-space grid search → local refinement) is more robust than pure gradient descent for H,G1,G2
  - Taxonomy centroids from Penttila et al. 2016: S(0.25,0.21), C(0.82,0.02), X(0.58,0.10), B(0.09,0.55)
  - Phase integral formula: q = 0.009082 + 0.4061*G1 + 0.8768*G2
  - `from prospector.spectral.phase_curve import fit_phase_curve, fit_all, h_g1g2_model`
  - `fit_all(conn)` reads from `mpc_photometry` table (user-created), writes to `phase_curve` table
---

## 2026-02-24 — near-earth-e5r: spin-barrier-filter
- Created `prospector/ingest/lcdb.py`: LCDB rotation period ingestion + spin-barrier classification
- Key functions: `ingest_lcdb()`, `classify_spin()`
- Added `rotation_properties` table to `prospector/db.py` (9 columns: period, amplitude, quality, is_monolithic, is_binary_suspect)
- Added `compute_spin_modifier()` to `prospector/scoring/scorer.py` — multiplicative factor (1.15 monolithic / 0.80 binary / 1.0 default)
- Updated `score_asteroid()` with `is_monolithic` and `is_binary_suspect` kwargs
- Updated `score_all()` to LEFT JOIN rotation_properties and pass spin data to scorer
- Updated `tests/test_db.py` EXPECTED_TABLES, `tests/test_scoring_scorer.py` required keys
- 39 tests in `tests/test_ingest_lcdb.py`: helpers, spin classification, spin modifier, ingest integration (designation fallback, quality filter, idempotent reingest), scorer integration
- Files changed: `prospector/db.py`, `prospector/ingest/lcdb.py` (new), `prospector/scoring/scorer.py`, `tests/test_ingest_lcdb.py` (new), `tests/test_db.py`, `tests/test_scoring_scorer.py`
- **Learnings for future iterations:**
  - LCDB quality codes: '0','1','2','2+','3' — ordinal ranking, U=3 is most reliable
  - Spin barrier at 2.2 hr is Warner et al. (2009) — objects below are monolithic (no rubble pile can hold together)
  - Binary suspects: P > 6.0 hr AND amplitude > 0.7 mag — high amplitude from elongated shape or eclipsing binary
  - `from prospector.ingest.lcdb import ingest_lcdb, classify_spin`
  - Modifier is multiplicative on composite_score, not additive — preserves relative ranking among similarly-structured bodies
---

## 2026-02-24 — near-earth-6v8: thermal-depletion-filter
- Added `compute_thermal_depletion_factor(q_au)` to `prospector/scoring/scorer.py`
- Logistic sigmoid on perihelion distance: center=0.6 AU, steepness=8, safe above 1.0 AU
- Applied as multiplicative penalty to water grades only inside MC loop — PGM/iron/silicate unaffected
- Added `q_au` parameter to `score_asteroid()`; auto-derived from a*(1-e) when not explicit
- Updated `score_all()` to fetch `o.q` from orbits table and pass to scorer
- Returns `thermal_depletion_factor` in result dict
- 10 new tests in `tests/test_scoring_scorer.py`: unit tests for factor function + integration tests
- Files changed: `prospector/scoring/scorer.py`, `tests/test_scoring_scorer.py`
- **Learnings for future iterations:**
  - Material-specific modifiers go inside the MC material loop (unlike spin_modifier which is global)
  - `if material == "water": grade_frac *= thermal_depl` — selective penalty pattern
  - q_au derived from a*(1-e) when not provided explicitly — explicit q from orbits table preferred
  - Factor values: q=0.2→0.04, q=0.5→0.31, q=0.6→0.50, q=0.8→0.83, q=1.0→1.00
  - `from prospector.scoring.scorer import compute_thermal_depletion_factor`
---

## 2026-02-24 — near-earth-0wf: ssodnet-ingest
- Implemented SsODNet (IMCCE Solar System Open Database Network) supplementary data ingestion
- Created `prospector/ingest/ssodnet.py`: REST API client (`fetch_ssocard`), JSON parser (`parse_ssocard`), two ingestion paths (live API and pre-downloaded JSON)
- Added `ssodnet_properties` table to `prospector/db.py`: mass, density, thermal inertia, taxonomy, delta-v
- Files changed: `prospector/db.py`, `prospector/ingest/ssodnet.py`, `tests/test_db.py`, `tests/test_ingest_ssodnet.py`
- Tests: 37 new tests (unit tests for helpers, parser tests, JSON ingestion, mocked API tests, schema tests)
- **Learnings for future iterations:**
  - SsODNet API uses `{value: X, error: {min: -Y, max: Y}}` format — `_safe_float()` must handle both dicts and plain numbers
  - SsODNet ssoCard response nests under `parameters.physical.*` and `parameters.dynamical.*`
  - Mass is in kg, density in kg/m³, thermal_inertia in J/m²/s^0.5/K — all SI units
  - Taxonomy includes class, scheme (e.g., "Mahlke"), complex, and waverange
  - Delta-v pre-computed by SsODNet using Taylor+2018 method — useful supplementary accessibility metric
  - Rate limiting at 0.5s between requests is polite for IMCCE servers
  - `INSERT OR REPLACE` pattern works for ssodnet_properties (same as other ingest modules)
---

## 2026-02-24 — near-earth-yk5: literal-types
- Defined `ScoringMode = Literal['earth_return', 'in_space']` and `ObservationType = Literal['vnir_spectroscopy', 'vis_spectroscopy', 'radar', 'albedo']` in `prospector/schemas.py`
- Updated Pydantic models: `ScoringResult.score_mode` and `EVOIResultSchema.best_observation` use Literal types
- Updated `EVOIResult` dataclass `best_observation` field to use `ObservationType`
- Annotated all `mode` parameters in `scorer.py` and `evoi.py` with `ScoringMode`
- Annotated `_score_distribution`, `compute_evoi`, `compute_evoi_for_asteroid`, `rank_all` mode params
- Added `tests/test_literal_types.py` with 6 tests: valid/invalid mode, valid/invalid observation, integration tests for `score_asteroid` and `compute_evoi`
- Fixed pre-existing `max(..., key=dict.get)` mypy errors in both scorer.py and evoi.py (replaced with lambda)
- Fixed `compute_evoi` return type from `dict[str, float]` to `dict[str, Any]` (contains mixed types)
- Files changed: `prospector/schemas.py`, `prospector/scoring/scorer.py`, `prospector/scoring/evoi.py`, `tests/test_literal_types.py`
- **Learnings for future iterations:**
  - Adding type annotations to previously fully-untyped functions causes mypy to analyze their bodies, surfacing many cascading `no-untyped-call` errors from other untyped functions. The pre-existing mypy error count on these files rose from 4 to ~58, all cascading issues.
  - `max(dict, key=dict.get)` is a mypy arg-type error — use `key=lambda k: dict[k]` instead
  - Literal types defined in `schemas.py` are the single source of truth; import `ScoringMode`/`ObservationType` from there
  - Pydantic validates Literal types at runtime — invalid strings raise `ValidationError` with clear messages
---

## 2026-02-24 — near-earth-m6g: deterministic-snapshots
- Created tests/test_deterministic_snapshots.py with 13 snapshot tests
- Targets covered: taxonomy_prior, score_asteroid (Granvik + M-type), compute_evoi, full pipeline (score_all + rank_all)
- All snapshot values generated with np.random.default_rng(42) and inline expected dicts
- Tolerance: rel=1e-12 (bit-identical for seeded MC)
- Verified perturbation detection: changing _MOID_SCALE breaks accessibility snapshot
- Files changed: tests/test_deterministic_snapshots.py (new, 240 lines)
- **Learnings for future iterations:**
  - score_all and rank_all both use internal seed=42, so pipeline replays are deterministic without explicit RNG passing
  - EVOI values can be 0.0 when expected_std_post >= std_prior (clipped by max(0.0, ...))
  - Full pipeline test requires separate DB instances for repeated runs since score_all writes to scores table
  - Pre-existing: hypothesis module not installed, test_properties.py collection fails (unrelated)
---

## 2026-02-24 — near-earth-qfo: metamorphic-relations
- Added 11 Hypothesis metamorphic relation tests in `TestMetamorphicRelations` class
- Covers all 8 specified MRs: diameter/MOID monotonicity, score non-negative, thermal depletion at score level, posterior validity (4 tests), confidence monotonicity, VNIR>vis concentration, EVOI bounded by std
- Tests use paired RNG seeds for fair comparison, class-level config loading, `deadline=None` for EVOI
- Files changed: tests/test_properties.py (added ~230 lines, TestMetamorphicRelations class + new imports)
- **Learnings for future iterations:**
  - EVOI MC estimates are unreliable at low n_samples (50). With uniform prior and certain RNG seeds, EVOI computes as 0 because noise pushes expected_std_post > std_prior. Test mathematical prerequisites (confidence, posterior concentration) rather than direct EVOI comparison.
  - `_score_distribution` in evoi.py has a performance bug: calls `score_asteroid` (full MC run) then runs its OWN MC loop, doubling the work. The `score_asteroid` result is unused.
  - For C-type pure prob_vector, only water has nonzero recoverability (no PGM/iron/olivine/pyroxene for C). This makes thermal depletion monotonicity exact (not just statistical).
  - `hypothesis` package was not installed; needed `pip install hypothesis` first.
---

## 2026-02-24 — near-earth-737: jwst-water-detection
- Added test suite for JWST 6μm molecular water detection ingestion (47 tests)
- Fixed mypy type annotation issues in `prospector/ingest/jwst_water.py` (added `Any` annotations to `_safe_float`, `_safe_int`, `_parse_bool`; added explicit `dict[str, str | None]` annotation to `_resolve_columns`)
- Files changed: `prospector/ingest/jwst_water.py` (type fixes), `tests/test_ingest_jwst_water.py` (new)
- Tests cover: helper functions (_safe_float, _safe_int, _parse_bool, _resolve_columns), CSV ingestion (basic, designation lookup, aliases, idempotent re-ingest, error cases), JSON ingestion, schema validation, designation lookup
- **Learnings for future iterations:**
  - JWST water data is sparse (~4 asteroids now, 30+ in Cycle 2+) — ingestion handles curated CSV/JSON, not bulk catalogs
  - Column alias resolution pattern (`_resolve_columns` + `_COLUMN_ALIASES`) is reusable across ingest modules
  - Non-detections (detection=false) should be stored for completeness — negative results are scientifically valuable
---

## 2026-02-24 — near-earth-i8x: Pipeline runner CLI entry point
- Created `prospector/pipeline.py` — full orchestrator with data file auto-discovery and stage runners (ingest, spectral, scoring, output)
- Created `prospector/__main__.py` — thin CLI wrapper enabling `python -m prospector`
- CLI flags: `--data-dir`, `--db-path`, `--mode`, `--n-samples`, `--output`, `--stages`, `--list-data`, `-v`
- Files changed: `prospector/pipeline.py` (new), `prospector/__main__.py` (new), `tests/test_pipeline.py` (new, 16 tests)
- **Learnings for future iterations:**
  - `_INGEST_SOURCES` dict maps source names to subdirs + glob patterns for auto-discovery
  - `run_pipeline()` returns `dict[str, dict[str, int]]` — stage name → result counts
  - Lazy imports inside stage runners avoid importing all modules when running partial stages
  - mypy needs explicit `str()` cast for dict values used in `Path /` operations
---

## 2026-02-24 — near-earth-e39: FastAPI app skeleton + auth middleware
- Created `prospector/api/` package: `app.py`, `auth.py`, `models.py`
- FastAPI app factory with async lifespan for DB connection management
- API key auth via `X-API-Key` header or `api_key` query param, loaded from `PROSPECTOR_API_KEYS` env var
- Health endpoint at `/health` (unauthenticated), placeholder `/v1/status` (authenticated)
- Added `fastapi>=0.110` and `uvicorn[standard]>=0.27` to `pyproject.toml` optional deps (`api` extra)
- Files: `prospector/api/{__init__,app,auth,models}.py` (new), `tests/test_api.py` (new, 19 tests), `pyproject.toml` (modified)
- **Learnings for future iterations:**
  - SQLite + FastAPI TestClient requires `check_same_thread=False` — TestClient runs requests in a worker thread
  - Use `dependency_overrides[get_db]` to inject test database connections
  - Lifespan context manager handles DB open/close; re-opens with `check_same_thread=False` for async compat
  - `PRAGMA query_only = ON` enforces read-only at the API layer
---

## 2026-02-24 — near-earth-bj4: Rankings endpoint
- Created `prospector/api/routers/rankings.py` — GET /v1/rankings with pagination
- Query params: mode, limit (1-500), offset, min_score, material
- Reuses JOIN pattern from `output.py:_fetch_candidates()` but with offset+count support
- Files: `prospector/api/routers/{__init__,rankings}.py` (new), `prospector/api/app.py` (router registration), `tests/test_api_rankings.py` (16 tests)
- **Learnings for future iterations:**
  - `scores` table has `asteroid_id` as PK — each asteroid can only have one score row per mode (can't have both earth_return and in_space in same row)
  - API router pattern: create router in `routers/` module, `app.include_router()` in `create_app()`
  - Use `_build_where()` helper to compose SQL WHERE clauses with parameterized queries
---

## 2026-02-24 — near-earth-30y: Asteroid detail endpoint
- Created `prospector/api/routers/asteroids.py` — GET /v1/asteroids/{asteroid_id}
- Full JOIN across 8 tables: asteroids, orbits, physical_properties, taxonomy, band_analysis, cnn_mineral, pgm_convergence, rotation_properties, scores
- Response groups data into structured sections (orbit, taxonomy, etc.); null sections when data unavailable
- Files: `prospector/api/routers/asteroids.py` (new), `tests/test_api_asteroids.py` (new, 11 tests)
- **Learnings for future iterations:**
  - `_row_to_detail()` pattern: flat SQL row → nested dict with null sections for missing JOINs
  - Check `if flat["key"] is not None` for nullable LEFT JOIN results before building section dicts
---

## 2026-02-24 — near-earth-o19: Asteroid search endpoint
- Added GET /v1/asteroids search to `prospector/api/routers/asteroids.py`
- Supports: q (prefix match), neo/pha booleans, min/max diameter, min/max moid, taxonomy_class, limit/offset
- Files: `prospector/api/routers/asteroids.py` (modified), `tests/test_api_search.py` (new, 17 tests)
- **Learnings for future iterations:**
  - SQLite LIKE is case-insensitive by default for ASCII; use COLLATE NOCASE for explicit guarantees
  - COALESCE(o.diameter, pp.diameter_km) handles dual diameter sources in WHERE and SELECT
---

## 2026-02-24 — near-earth-4ca: EVOI observation priority endpoint
- Created `prospector/api/routers/evoi.py` — GET /v1/evoi
- Computes EVOI on-the-fly via `rank_all()`, limited to 50 asteroids/request
- Gracefully returns empty results on computation failure
- Files: `prospector/api/routers/evoi.py` (new), `tests/test_api_evoi.py` (9 tests)
- **Learnings for future iterations:**
  - Mock `rank_all` at module level (`prospector.api.routers.evoi.rank_all`) — requires module-level import, not local import
  - EVOI endpoint is the most expensive API endpoint; consider adding a pre-computed evoi table in future
---

## 2026-02-24 — near-earth-iz7: Docker deployment
- Created `Dockerfile` (Python 3.12-slim, installs `.[api]`, uvicorn CMD, healthcheck)
- Created `docker-compose.yml` (data volume mount, env vars, port 8000)
- 12 structural validation tests in `tests/test_docker.py`
---

## 2026-02-24 — near-earth-0k5: Fix np.trapz deprecation
- Used `getattr(np, "trapezoid", np.trapz)` for forward/backward compat
- NumPy 1.26.4 installed (pre-2.0), so np.trapezoid not yet available
- Eliminated 25 DeprecationWarning from test suite
---

## 2026-02-24 — near-earth-piu: Hypothesis stateful testing for ingest→score pipeline
- Created `tests/test_stateful.py`: RuleBasedStateMachine fuzzing the ingest→score pipeline
- 7 rules: ingest_full_asteroid, ingest_partial_no_diameter, add_taxonomy, add_physical_properties, score_all_asteroids, score_single_asteroid, update_diameter
- 6 invariants checked after every step: scores_non_negative, accessibility_bounded, scored_asteroids_exist, taxonomy_vectors_normalized, mass_estimates_non_negative, scores_are_finite
- Tests interleaved ingests with partial data, repeated scoring in both modes, taxonomy additions, diameter updates
- Settings: max_examples=50, stateful_step_count=15, deadline=None
- Files changed: `tests/test_stateful.py` (new)
- **Learnings for future iterations:**
  - RuleBasedStateMachine `@initialize()` replaces `__init__` for setup — must reset all state tracking there
  - `@precondition(lambda self: ...)` guards rules that need prior state (e.g., scoring needs insertions first)
  - `note(f"...")` from hypothesis logs per-step debug info visible on failure — essential for debugging shrunk examples
  - Stateful tests with `score_all` need low `n_samples=50` to keep runtime reasonable (50 examples × 15 steps)
  - `conn.execute("INSERT OR REPLACE ...")` handles both new inserts and updates of existing asteroid data
  - prob_vector strategy: generate positive floats list, convert to np.array, divide by sum → guaranteed normalized
---

## 2026-02-24 — near-earth-a4d: Mutation testing gate (mutmut)
- Added `mutmut>=2.4` to `[project.optional-dependencies] dev` in pyproject.toml
- Configured `[tool.mutmut]` with `paths_to_mutate=prospector/`, `runner=pytest -x --tb=no -q`, `tests_dir=tests/`
- Created `scripts/mutmut-gate.sh`: runs mutmut on a specified module, parses results, fails if survival rate >10%
- Added mutation testing gate section to `prompt.md` with usage instructions
- Added 5 tests in `tests/test_mutmut_gate.py`: config validation, script existence/permissions, usage error, missing module error
- **Learnings for future iterations:**
  - mutmut writes results to `.mutmut-cache/` directory — may want to gitignore
  - The gate script treats timeouts and suspicious mutants as "caught" (not survived) — this is the standard convention
  - Research/tooling beads are exempt from the mutation gate; only beads touching `prospector/` modules need it
  - The `--no-progress` flag suppresses mutmut's progress bar, cleaner for CI/script output
---

## 2026-02-24 — near-earth-eib: deal contracts on scorer, db, and spectral modules
- Added `deal>=4.24` to `[project.optional-dependencies] dev` in pyproject.toml
- Added `@deal.pre`/`@deal.post` contracts to core functions:
  - `scorer.py`: estimate_mass_kg (diameter>=0, density>0, mass>=0, finite), compute_accessibility (result in [0.01,1.0]), compute_confidence (result in [0.01,1.0]), compute_thermal_depletion_factor (result in [0,1]), score_asteroid (diameter>0, composite_score>=0, finite)
  - `db.py`: get_connection (result not None), table_names (conn not None, result is list)
  - `preprocessing.py`: resample (array length match, >=2 points), normalize (array length match)
  - `taxonomy.py`: classify_spectrum (array length match, >=3 points)
  - `band_analysis.py`: analyze_spectrum (array length match, >=10 points), estimate_temperature (result>0)
- Added `deal.cases()` auto-generated tests in `tests/test_properties.py` (TestDealContracts class)
- Documented deal contract pattern in CLAUDE.md
- Files changed: pyproject.toml, prospector/scoring/scorer.py, prospector/db.py, prospector/spectral/preprocessing.py, prospector/spectral/taxonomy.py, prospector/spectral/band_analysis.py, tests/test_properties.py, CLAUDE.md, progress.txt
- **Learnings for future iterations:**
  - `deal.pre` lambda must accept same positional args as the function — use `*_`/`**__` to ignore extras
  - `deal.post` lambda receives only the return value — check it directly
  - `deal.cases(func, count=N, kwargs={...})` needs kwargs for complex types that Hypothesis can't auto-generate
  - Contracts are active by default — use `deal.disable(permanent=True)` for production zero-overhead
  - deal reduced mypy errors by 6 (type annotations on contracted functions trigger analysis of previously-untyped code)
  - Zero-diameter asteroid is a valid edge case for estimate_mass_kg (returns 0.0) — use `>=0` not `>0`
---

## 2026-02-24 — near-earth-kux: Config checksum gate for scoring YAML drift detection
- Implemented SHA-256 checksum validation for all 3 scoring YAML config files
- Created `prospector/scoring/config_schema.py` with:
  - `CONFIG_CHECKSUMS` dict mapping filenames to approved SHA-256 hashes
  - `ConfigDriftError` exception class
  - `validate_config_checksum()` and `validate_all_configs()` functions
  - Pydantic schemas (`DistributionWithSource`, `DensityPriors`, `GradeSection`, `GradeDensityConfig`) requiring `source` citation fields on all distribution entries
- Integrated checksum validation into `scorer.py:load_config()` — validates on every config load
- Added 5 tests to `test_deterministic_snapshots.py::TestConfigChecksumGate`:
  - `test_all_configs_pass_validation` — all 3 YAMLs match approved hashes
  - `test_individual_checksum_matches` — parametrized per-file independent hash check
  - `test_drift_detected_on_tampered_file` — ConfigDriftError on modified content
  - `test_unknown_config_raises_key_error` — rejects unknown config filenames
  - `test_missing_file_raises_file_not_found` — catches missing configs
- Updated CLAUDE.md with config checksum gate pattern and critical constraint
- Files changed: prospector/scoring/config_schema.py (new), prospector/scoring/scorer.py, tests/test_deterministic_snapshots.py, CLAUDE.md, progress.txt
- **Learnings for future iterations:**
  - Scoring YAMLs are now locked — any modification requires updating CONFIG_CHECKSUMS in a dedicated bead
  - `load_config()` validates checksum on every call, so all downstream consumers (scorer, EVOI) get protection automatically
  - mutmut 3.x has incompatible CLI with the `mutmut-gate.sh` script (uses `--paths-to-mutate` which was removed); mutmut 2.5.x works
  - Pydantic `DistributionWithSource` schema enforces citations at parse time, not just at test time
---

## 2026-02-24 — near-earth-bai: Pipeline stage Protocol with pre/postcondition checks
- Defined `PipelineStage` Protocol in `prospector/pipeline.py` with 3 methods: `preconditions_met(db)`, `run(db)`, `postconditions_met(db)`
- `StageResult` dataclass holds stage name + counts dict
- `@runtime_checkable` enables `isinstance()` checks at test time
- Implemented 4 concrete stage classes: `IngestStage`, `SpectralStage`, `ScoringStage`, `OutputStage`
- Pre/postcondition checks query DB tables: `_table_has_rows()` helper wraps `SELECT 1 ... LIMIT 1`
- Preconditions: Ingest=always True, Spectral=spectra table has rows, Scoring=asteroids+orbits, Output=scores
- Postconditions: Ingest=asteroids table has rows, Spectral=taxonomy table, Scoring=scores table, Output=CSV file exists
- 27 tests in `tests/test_pipeline_contracts.py`: protocol conformance (isinstance), preconditions, postconditions, StageResult, sequential integration
- Files changed: `prospector/pipeline.py`, `tests/test_pipeline_contracts.py` (new)
- **Learnings for future iterations:**
  - `@runtime_checkable` + `Protocol` from `typing` — requires `name` attribute + 3 methods for isinstance
  - `_table_has_rows()` uses try/except for nonexistent tables — returns False gracefully
  - Schema column names: spectra table uses `survey` (not `source`), taxonomy uses `primary_class`/`primary_prob`/`classifier` (not `tax_class`/`prob`/`method`)
  - Stage classes delegate to existing `_run_*()` functions — no duplicated orchestration logic
  - `from prospector.pipeline import PipelineStage, StageResult, IngestStage, SpectralStage, ScoringStage, OutputStage`
---

## 2026-02-24 — near-earth-70u: Create project README
- Created README.md with: project overview, installation, pipeline CLI usage, API/Docker instructions, test commands, project structure, key references, and GPL-3.0 license
- Files changed: README.md (new)
- Tests: all 1437 pass (no new tests needed — docs-only bead)
- **Learnings for future iterations:**
  - LICENSE file is GPL-3.0 despite pyproject.toml declaring MIT — used GPL-3.0 as canonical
  - API uses factory pattern: `uvicorn prospector.api.app:create_app --factory`
---

## 2026-02-25 — near-earth-bw8: Config lookup arrays
- Added `ConfigArrays` dataclass and `build_config_arrays()` to `prospector/scoring/scorer.py`
- Converts nested YAML config dicts into flat `np.ndarray(17,)` indexed by Mahlke class index
- Arrays: density (mean/std/min/max), grade per material (mean/std/min/max), unit_factors, recoverability
- Missing class-material combos filled with zeros; density uses `_FALLBACK_DENSITY` for missing classes
- Helper functions: `_unit_to_factor()` (ppm→1e-6, wt_pct→0.01), `_dist_to_arrays()` (dict→4 arrays)
- 16 tests in `tests/test_scoring_scorer.py::TestConfigArrays`
- Files changed: `prospector/scoring/scorer.py`, `tests/test_scoring_scorer.py`
- **Learnings for future iterations:**
  - `_CLASS_TO_IDX` dict maps class name → index for O(1) lookup during array construction
  - ConfigArrays is frozen (immutable) — build once, pass to vectorized MC loop
  - Recoverability defaults to `DEFAULT_RECOVERABILITY` dict when config lacks `recoverability_defaults`
  - `from prospector.scoring.scorer import ConfigArrays, build_config_arrays`
  - Downstream vectorization bead (near-earth-181) should use these arrays with fancy indexing: `density_mean[class_indices]`
---

## 2026-02-25 — near-earth-181: Vectorize score_asteroid inner MC loop
- Replaced `for i in range(n_samples)` loop in `score_asteroid()` with vectorized numpy operations
- Pattern: `rng.choice(17, size=n_samples, p=pv)` → fancy-indexed density/grade draws → vectorized mass → 5-material accumulation loop
- Built recoverability arrays from dict parameter (not ConfigArrays) to preserve caller override support
- Vectorized `best_grade` computation: `np.sum(pv * ca.grade_mean[material] * ca.unit_factors[material])`
- Re-captured all snapshot expected values in `test_deterministic_snapshots.py` (RNG draw order changes with vectorization)
- Files changed: `prospector/scoring/scorer.py`, `tests/test_deterministic_snapshots.py`
- All 1453 tests pass (including properties, stateful, and snapshots)
- **Learnings for future iterations:**
  - Vectorizing changes RNG consumption order (batched vs interleaved) — snapshot re-capture is mandatory
  - `rng.normal(loc, scale=0.0)` returns `loc` — safe for zero-std entries in ConfigArrays
  - Custom `recoverability` dict override requires building arrays outside ConfigArrays
  - 5-material inner loop is acceptable — vectorizing across materials would require 3D arrays for marginal gain
  - `ca.unit_factors[material]` is equivalent to `_grade_to_fraction()` — multiply instead of divide
---

## 2026-02-25 — near-earth-ve4: Per-asteroid SeedSequence seeding in score_all
- Replaced shared `rng = default_rng(42)` with per-asteroid `SeedSequence(42, spawn_key=(asteroid_id,))`
- Each asteroid's score is now deterministic and independent of other asteroids in DB
- Added `ORDER BY a.asteroid_id` to the scoring query for deterministic row ordering
- Built ConfigArrays once before the loop and pass via `_config_arrays` kwarg to `score_asteroid`
- Added `_config_arrays=None` kwarg to `score_asteroid` — skips `build_config_arrays` rebuild when provided
- New test: `test_score_independence_of_extra_asteroid` — inserting asteroid ID=0 doesn't change existing scores
- Re-captured all pipeline replay snapshot values in `test_deterministic_snapshots.py`
- Files changed: `prospector/scoring/scorer.py`, `tests/test_deterministic_snapshots.py`
- All 1454 tests pass
- **Learnings for future iterations:**
  - `SeedSequence(42).spawn(N)` is index-dependent — spawn(5)[0] == spawn(6)[0] but row indices shift when new asteroids are inserted
  - `SeedSequence(42, spawn_key=(asteroid_id,))` gives true DB-independence — seed derived from asteroid_id, not row position
  - `_config_arrays` kwarg avoids rebuilding ConfigArrays per asteroid in score_all — O(1) instead of O(N) YAML-to-array conversions
  - EVOI ranking is not affected by score_all changes (separate code path with its own RNG)
---

## 2026-02-25 — near-earth-w0l: Vectorize EVOI _score_distribution + extract shared MC kernel
- Extracted `_vectorized_mc_samples()` kernel from `score_asteroid` into standalone function
- Kernel returns `(sample_values, mass_samples, material_totals)` before global multipliers
- `score_asteroid` calls kernel and applies confidence × accessibility × spin_modifier
- `_score_distribution` (evoi.py) refactored from 60-line duplicate loop to 5-line kernel call
- Removed wasteful `score_asteroid()` call in old `_score_distribution` that consumed RNG without using result
- Applied SeedSequence(42, spawn_key=(asteroid_id,)) in `rank_all` with ORDER BY a.asteroid_id
- Re-captured all EVOI snapshot values in `test_deterministic_snapshots.py`
- Files changed: `prospector/scoring/scorer.py`, `prospector/scoring/evoi.py`, `tests/test_deterministic_snapshots.py`
- All 1454 tests pass
- **Learnings for future iterations:**
  - `_vectorized_mc_samples` is the shared MC kernel — import from `prospector.scoring.scorer`
  - Old `_score_distribution` called `score_asteroid()` then duplicated its own loop — wasteful pattern now fixed
  - Kernel takes `thermal_depl`/`jwst_boost` with defaults of 1.0 — EVOI doesn't use thermal/water modifiers
  - `_config_arrays` kwarg avoids rebuilding ConfigArrays per call — pass to kernel when pre-built
---

## 2026-02-26 — near-earth-uew: CrossHair SMT backend for existing Hypothesis tests
- Added `hypothesis-crosshair>=0.0.27` to `pyproject.toml[dev]` dependencies
- Created `tests/test_crosshair_scoring.py` with 13 CrossHair SMT-backed property tests
- 7 pure-Python functions tested (no numpy/scipy internals):
  - `estimate_mass_kg` — non-negativity, d³ scaling
  - `compute_thermal_depletion_factor` — unit interval bounds, monotonicity
  - `classify_gaffey_subtype` — always returns valid S(I)–S(VII) subtype
  - `dunn_calibration` — ol_ratio bounded [0,1], fa non-negative
  - `lindsay_bar_correction` — corrected BAR preserves sign
  - `temperature_correct_band_centers` — round-trip recovery
  - `compute_spin_modifier` — monolithic bonus, binary penalty, neutral default, priority
- Registered `crosshair` pytest marker in `pyproject.toml` for nightly CI runs
- Files changed: `pyproject.toml`, `tests/test_crosshair_scoring.py` (new)
- **Learnings for future iterations:**
  - CrossHair module is `hypothesis_crosshair_provider` (not `hypothesis_crosshair`) — auto-registers via entry point
  - `@settings(backend='crosshair')` is all you need — no explicit import of CrossHair required
  - z3-solver pip package ships arm64 dylib on macOS — if Python runs under Rosetta (x86_64), download the x86_64 wheel: `pip download z3-solver --platform macosx_12_0_x86_64 --python-version 3.12 --only-binary=:all:`
  - Use `suppress_health_check=[HealthCheck.too_slow]` and `deadline=None` for CrossHair tests — SMT solving is slower than random sampling
  - Functions using `np.sqrt`, `np.clip`, or any numpy/scipy are NOT SMT-amenable — CrossHair falls back to concrete execution, negating the SMT benefit
  - `max_examples=20` is sufficient for CrossHair — SMT explores paths exhaustively, not statistically
---

## 2026-02-26 — near-earth-fah: PBT for Lambert solver orbital mechanics invariants
- Added `TestLambertProperties` class in `tests/test_ephemeris_lambert.py` with 7 `@given`-decorated property tests
- Properties tested:
  1. Vis-viva theorem: v² = μ(2/r - 1/a) for random (a, e, M)
  2. Kepler equation round-trip: E - e·sin(E) = M for random (M, e)
  3. Stumpff C(z) continuity at Taylor/exact branch boundary near z=0
  4. Stumpff S(z) continuity at Taylor/exact branch boundary near z=0
  5. departure_dv monotonicity: higher v_inf → higher Δv
  6. solve_lambert energy conservation: ε₁ = ε₂ at both endpoints
  7. earth_state radius always in [0.97, 1.03] AU for ±50 year range
- Files changed: `tests/test_ephemeris_lambert.py`
- All 54 tests pass (47 existing + 7 new PBT)
- **Learnings for future iterations:**
  - Stumpff continuity is NOT symmetry — C(z) is not symmetric around z=0. Test branch agreement, not C(ε)≈C(-ε)
  - Lambert solver returns None for degenerate geometry — use `assume(result is not None)` in PBT
  - `deadline=None` needed for compute-heavy PBTs; `max_examples=100-300` gives good coverage
  - For monotonicity tests, `assume(abs(x1 - x2) > threshold)` avoids flaky near-equal comparisons
---
